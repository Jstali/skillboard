# Database Configuration for Local Development
# Copy this file to .env and adjust values as needed

# For local PostgreSQL installation
DATABASE_URL=postgresql://postgres:stali@localhost:5432/skillboard

# For Docker PostgreSQL (if using docker-compose up postgres)
# DATABASE_URL=postgresql://skillboard:skillboard@localhost:2609/skillboard

# For Docker Compose (connecting to postgres service)
# DATABASE_URL=postgresql://skillboard:skillboard@postgres:5432/skillboard
